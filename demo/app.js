/**
 * Demo app for onion-boarding.
 *
 * As the app itself is self-contained and just looks for an id tag on the page to take over,
 * all we need to do here is import the app to let webpack bundle it.
*/
import '../src/app.js';
